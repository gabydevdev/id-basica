(function ($) {

	// if (typeof acf.addAction !== 'undefined') {

	// 	// console.log('acf.addAction is defined');


	// 	acf.addAction('new_field/name=fecha_de_formato', function (field) {
	// 		console.log('Field fecha_de_formato is ready', field);
	// 		console.log('Field data:', field.data);
	// 		console.log('Field $el:', field.$el);
	// 		console.log('Field $control:', field.$control());
	// 		console.log('Field $input:', field.$input());

	// 		field.$el.find('input').prop('disabled', true);

	// 		// date_format:"dd/mm/yy"

	// 		// Fill input with current date in dd/mm/yy format
	// 		var now = new Date();
	// 		var day = String(now.getDate()).padStart(2, '0');
	// 		var month = String(now.getMonth() + 1).padStart(2, '0');
	// 		var year = String(now.getFullYear()).slice(-2);
	// 		var formattedDate = day + '/' + month + '/' + year;
	// 		field.$el.find('input').val(formattedDate);

	// 	});

	// }
})(jQuery);
